{
  "name": "socialbot-backend",
  "version": "1.0.0",
  "description": "SocialBot Backend API",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app-simple.js",
    "dev": "node src/app.js",
    "build": "echo 'No build needed for simplified version'",
    "postinstall": "echo 'No postinstall needed for simplified version'",
    "db:generate": "npx prisma generate",
    "db:push": "npx prisma db push",
    "start:full": "node src/app.js"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "joi": "^17.11.0",
    "jsonwebtoken": "^9.0.2",
    "node-fetch": "^2.6.7",
    "prisma": "^5.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
